{
  "name": "build-your-agentic-team",
  "version": "1.0.0",
  "private": true,
  "description": "A marketing organization as a repository. Every function is a markdown file; the diagram is generated from them.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/build.mjs --check",
    "serve": "node scripts/build.mjs && node --run serve:only",
    "serve:only": "cd site && python3 -m http.server 8000"
  },
  "license": "UNLICENSED"
}
